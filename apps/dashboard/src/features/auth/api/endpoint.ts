/**
 * API Endpoint Configuration : ce fichier contient les URL des apis utilisées dans une feature
 * Cela permet de centraliser les URL des apis.
 */

export const AUTH_ENDPOINT = {
    // Example :  URL de l'api pour récupérer la liste des utilisateurs
    LOGIN: '/auth/login'
} as const;
